#include "recursiveStrings.h"

void expand(char **current, int bufferSize, char **axiomv, int axiomc) {

  int i;
  for (i = 0; i < axiomc; i++) {
    int j = 0;

    while (j < bufferSize) {

      j++;
    }
  }
}
